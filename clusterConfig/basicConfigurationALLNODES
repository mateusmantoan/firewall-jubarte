#basic instalation for all nodes of cluster

#script off official docker site
DOCKER_SCRIPT=https://get.docker.com

#install packets
apt-get update && apt-get install -y curl \
apt-transport-https \
iproute2 \
iptables-persistent \
sshpass
curl -fsSL $DOCKER_SCRIPT | bash
apt clean

#configure ssh for remote access
echo "PermitRootLogin yes" >> /etc/ssh/sshd_config
systemctl restart ssh

#create manager files
touch /etc/hostsfile
touch /etc/iptables-actual
touch /etc/iptables-actual_TEMP
touch /etc/snort-status
touch /etc/squid-status
mkdir /etc/logs/
mkdir /etc/logs/tcpdump
mkdir /etc/logs/nmap
mkdir /etc/logs/snort
